<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:id="@+id/add_device" android:icon="@android:drawable/ic_menu_add"
            android:title="@string/import_label" />
        <item android:id="@+id/manager_group" android:icon="@android:drawable/ic_menu_agenda"
            android:title="@string/gallery_label" />

        <item android:id="@+id/manager_credentials" android:icon="@android:drawable/ic_lock_idle_lock"
            android:title="@string/credentials_label" />
    </group>

    <item android:title="Communicate">
        <menu>
            <item android:id="@+id/send_trap" android:icon="@android:drawable/ic_menu_share"
                android:title="@string/trap_action" />
        </menu>
    </item>

</menu>